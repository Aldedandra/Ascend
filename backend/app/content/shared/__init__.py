"""Shared curriculum helpers and reusable content definitions.

This package is intentionally empty for now. Add shared builders only when
multiple lessons need the same behavior; avoid abstractions for their own sake.
"""
